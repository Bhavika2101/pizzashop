// Test generated by RoostGPT for test PizzaShop using AI Type Open AI and AI Model gpt-4

const ResourceNotFoundException = require('./ResourceNotFound');

describe('ResourceNotFoundException', () => {
  let error;

  beforeEach(() => {
    error = new ResourceNotFoundException('Test error message');
  });

  test('sets correct status', () => {
    expect(error.status).toBe('Not found');
  });

  test('sets correct code', () => {
    expect(error.code).toBe('404');
  });

  test('sets correct title', () => {
    expect(error.title).toBe('Resource does not exist');
  });

  test('sets correct description', () => {
    expect(error.description).toBe('Test error message');
  });

  test('captures stack trace', () => {
    expect(error.stack).toBeDefined();
  });
});
